#######################################
# LOCATIONS
#######################################
IPS_PATH = /Users/m4c/ips_framework_install/bin

#########################################
#  ips-template Locations
#  Path to the ips-vmec component scripts.
IPS_TEMPLATE_COMP_PATH = /Users/m4c/Research/IPS/IPS-Wrappers/ips-template

#  sleep Install Path and name. Install path is the path to the directory where
#  sleep is installed. Install name is the name of the sleep executable.
IPS_SLEEP_INSTALL_PATH = /bin
IPS_SLEEP_INSTALL_NAME = sleep

#######################################
# Parallel environment
#######################################
MPIRUN = mpiexec
NODE_DETECTION = manual
CORES_PER_NODE = 4
SOCKETS_PER_NODE = 1
NODE_ALLOCATION_MODE = shared

#######################################
# Provenance
#######################################
HOST = mbp103254
USER = m4c
HOME = /Users/m4c
SCRATCH = 

###
## DOCUMENTATION
#
#######################################
# NODE_ALLOCATION_MODE
#
# MUST ADHERE TO THE PLATFORM'S CAPABILITIES
#   * EXCLUSIVE : only one task per node
#   * SHARED : multiple tasks may share a node
# For single node jobs, this can be overridden allowing multiple
# tasks per node.
#
#######################################
# NODE_DETECTION =resource detection method
#
# OPTIONS = checkjob | qstat | pbs_env | slurm_env
#
#######################################
# ips-solps-iter options.
#
# IPS_SOLPS_COMP_PATH = path to the installed locations of the ips-solps-iter scripts.
#
# IPS_SLEEP_PATH = path to the install location of sleep.
# IPS_SLEEP_NAME = name of the sleep binary.
#
