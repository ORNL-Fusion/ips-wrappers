#######################################
# LOCATIONS
#######################################
IPS_PATH = /Users/m4c/ipsinstall/bin


#########################################
# ips-ml-train Locations
#  Path to the ips-ml-train component scripts.
IPS_ML_TRAIN_COMP_PATH = /Users/m4c/ips-wrappers/ips-ml-train

#  ML Train Install Path and name. Install path is the path to the directory
#  where ml_train_exe.py is installed. Install name is the name of the
#  ml_train_exe.py executable.
ML_TRAIN_INSTALL_PATH = /Users/m4c/ips-wrappers/ips-ml-train
ML_TRAIN_INSTALL_NAME = ml_train_exe.py

#######################################
# Parallel environment
#######################################
MPIRUN = mpiexec
NODE_DETECTION = manual
CORES_PER_NODE = 4
SOCKETS_PER_NODE = 1
NODE_ALLOCATION_MODE = shared

#######################################
# Provenance
#######################################
HOST = mbp103254
USER = m4c
HOME = /Users/m4c
SCRATCH = 

USER_W3_DIR = /Users/m4c/ips-wrappers/ips-ml-train/Test/www

###
## DOCUMENTATION
#
#######################################
# NODE_ALLOCATION_MODE
#
# MUST ADHERE TO THE PLATFORM'S CAPABILITIES
#   * EXCLUSIVE : only one task per node
#   * SHARED : multiple tasks may share a node
# For single node jobs, this can be overridden allowing multiple
# tasks per node.
#
#######################################
# NODE_DETECTION =resource detection method
#
# OPTIONS = checkjob | qstat | pbs_env | slurm_env
#
#######################################
# ips-ml-train options.
#
# IPS_ML_TRAIN_COMP_PATH = path to the installed locations of the ips-ml-train scripts.
#
# ML_TRAIN_INSTALL_PATH = path to the install location of ML_TRAIN.
# ML_TRAIN_INSTALL_NAME = name of the ML_TRAIN binary.
#
