#######################################
# LOCATIONS
#######################################
IPS_PATH = /Users/m4c/ipsinstall/bin
IPS_WRAPPERS = /Users/m4c/ips-wrappers

#########################################
#  ips Locations
#  Path to the ips component scripts.
IPS_VMEC_COMP_PATH = $IPS_WRAPPERS/ips-vmec
IPS_SIESTA_COMP_PATH = $IPS_WRAPPERS/ips-siesta
IPS_V3FIT_COMP_PATH = $IPS_WRAPPERS/ips-v3fit
IPS_QUASI_NEWTON_COMP_PATH = $IPS_WRAPPERS/ips-quasi-newton

VMEC_CONFIG = $IPS_VMEC_COMP_PATH/Test/ips.vmec.config
SIESTA_CONFIG = $IPS_SIESTA_COMP_PATH/Test/ips.siesta.config
V3FIT_CONFIG = $IPS_V3FIT_COMP_PATH/Test/ips.v3fit.config

#  VMEC Install Path and name. Install path is the path to the directory where
#  where VMEC is installed. Install name is the name of the VMEC executable.
VMEC_INSTALL_PATH = /Users/m4c/repo/trunk/build/bin
VMEC_INSTALL_NAME = xvmec

#  VMEC Install Path and name. Install path is the path to the directory where
#  where SIESTA is installed. Install name is the name of the SIESTA executable.
SIESTA_INSTALL_PATH = /Users/m4c/repo/trunk/build/bin
SIESTA_INSTALL_NAME = xsiesta

#  V3FIT Install Path and name. Install path is the path to the directory where
#  where VMEC is installed. Install name is the name of the V3FIT executable.
V3FIT_INSTALL_PATH = /Users/m4c/repo/trunk/build/bin
V3FIT_INSTALL_NAME = xv3fit

#######################################
# Parallel environment
#######################################
MPIRUN = mpiexec
NODE_DETECTION = manual
CORES_PER_NODE = 4
SOCKETS_PER_NODE = 1
NODE_ALLOCATION_MODE = shared

#######################################
# Provenance
#######################################
HOST = mbp103254
USER = m4c
HOME = /Users/m4c
SCRATCH = 

USER_W3_DIR = $IPS_QUASI_NEWTON_COMP_PATH/Test/www
HTML_DUMP_FREQ = 100

###
## DOCUMENTATION
#
#######################################
# NODE_ALLOCATION_MODE
#
# MUST ADHERE TO THE PLATFORM'S CAPABILITIES
#   * EXCLUSIVE : only one task per node
#   * SHARED : multiple tasks may share a node
# For single node jobs, this can be overridden allowing multiple
# tasks per node.
#
#######################################
# NODE_DETECTION =resource detection method
#
# OPTIONS = checkjob | qstat | pbs_env | slurm_env
#
#######################################
# ips-vmec options.
#
# IPS_VMEC_COMP_PATH = path to the installed locations of the ips-vmec scripts.
#
# VMEC_INSTALL_PATH = path to the install location of VMEC.
# VMEC_INSTALL_NAME = name of the VMEC binary.
#
# ips-siesta options.
#
# IPS_SIESTA_COMP_PATH = path to the installed locations of the ips-siesta scripts.
#
# SIESTA_INSTALL_PATH = path to the install location of SIESTA.
# SIESTA_INSTALL_NAME = name of the SIESTA binary.
#
# ips-v3fit options.
#
# IPS_V3FIT_COMP_PATH = path to the installed locations of the ips-v3fit
# scripts.
#
# V3FIT_INSTALL_PATH = path to the install location of V3FIT.
# V3FIT_INSTALL_NAME = name of the V3FIT binary.
#
# ips-quasi-newton options.
#
# IPS_QUASI_NEWTON_COMP_PATH = path to the installed locations of the
# ips-quasi-newton scripts.
#
